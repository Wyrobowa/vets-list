extends layout

include mixins/_registerForm

block content
    .form-group
        button.btn.btn-info.btn-sm(onclick="history.back(-1)")
            i.fas.fa-arrow-left
            span  Back
            
    .form-box
        +registerForm()
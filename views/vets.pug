extends layout

include mixins/_vetCard

block content
    h2.title #{title}

    .row
        each vet in vets
            .col-6.col-md-4
                .card.mb-3
                    +vetCard(vet)


